/**
 * Permission keys generated from the module's permissions tree.
 */
export const ResumePermission = {
  key: "resume.*",
  name: "resume",
  title: { en: "Resume" },
  description: {
    en: "Manage every resume section (profiles, companies, work experience, education, skills, projects, certifications, languages, target positions).",
  },
} as const;
export const ResumePermissionList = [ResumePermission] as const;
